# Phase 0 — Shareable skeleton with stub generation

The point of Phase 0 is to validate the **data model, URL strategy, share/edit flow, and Phaser rendering loop** before any real API calls. All generation endpoints return canned placeholder sprites. If the share-URL flow works end-to-end with a fake character, swapping in nano banana in Phase 1 becomes a contained change.

## Stack lock-in

| Layer | Choice | Why |
|---|---|---|
| Frontend | Vite + React + TS + Phaser | Already validated; "shareable" doesn't change this |
| Server | Node + Hono (or Express) | Persistent WebSocket later for ElevenLabs streaming → rules out edge-only platforms |
| DB | SQLite via Drizzle (Turso for prod) | Simple, free tier, works locally and in prod with the same code |
| Asset storage | Cloudflare R2 | Cheapest egress for shared assets; S3-compatible API |
| Sprite generation | **PixelLab** (Phase 1+) | Purpose-built for pixel-art sprites; native animation + pose primitives; stub-only in Phase 0 |
| Portraits / refs | **Nano banana** (Gemini 2.5 Flash Image) (Phase 1+) | High-fidelity static portraits + multi-image reference fusion for character design; not used for sprites |
| Voice | ElevenLabs (Phase 2+) | TTS + streaming + alignment for lipsync |
| Hosting | Vercel (frontend) + Fly.io or Railway (Node API) | Vercel is ideal for the React app; the Node server lives separately so it can hold WebSockets |

**Aesthetic:** pixel art (see D07). The generator split (PixelLab for sprites, nano banana for portraits) is per D08, behind the generator abstraction in D09.

## URL & share model

```
/                          → landing / "create new" entry point
/c/:slug                   → public read-only viewer (anyone can see + play)
/c/:slug/edit?key=:editKey → editor (full inspector, generation, voice)
```

- `slug` = nanoid(8) + optional `-name-kebab` suffix (e.g. `/c/aB3kF9xQ-blue-wizard`)
- `editKey` = nanoid(24), generated on creation, stored as a cookie *and* embedded in the URL the creator sees
- No accounts in Phase 0. Lose the edit URL = lose edit access. Phase 3 can layer accounts on top by binding `editKey` → user.
- Public read works without cookies, edit key, or auth. This is what makes it "shareable from day one."

## Data model

Drizzle SQLite schema (Slice 1 implements directly from this — JSON columns use `text({ mode: 'json' })`, not `jsonb` which is Postgres):

```ts
characters: {
  id: text (nanoid pk)
  slug: text (unique idx)
  editKeyHash: text                          // hash, never store raw
  name: text
  basePrompt: text                           // user's freeform description
  refImageUrl: text | null                   // R2 URL (Phase 1+, unused in Phase 0)
  attributes: text({ mode: 'json' })         // { archetype, outfit, palette, expression, ... }
  portraitUrl: text | null                   // canonical portrait (illustrated, from nano banana)
  portraitGenerator: enum                    // "stub" | "nano-banana"
  portraitStatus: enum                       // "pending" | "ready" | "failed"
  voiceId: text | null                       // ElevenLabs voice id
  createdAt, updatedAt
}

poses: {
  id, characterId (fk → characters.id, ON DELETE CASCADE)
  name: text                                 // fixed vocab — see "Pose vocabulary" below
  spriteSheetUrl: text                       // R2 URL (pixel art, from PixelLab)
  manifest: text({ mode: 'json' })           // PoseManifest, shape below
  generator: enum                            // "stub" | "pixellab"
  status: enum                               // "pending" | "ready" | "failed"
}
```

The `PoseManifest` shape — uniform sprite-sheet layout that maps directly onto Phaser's `load.spritesheet` + `anims.create`:

```ts
type PoseManifest = {
  frameWidth: number   // 64 in Phase 0
  frameHeight: number  // 64 in Phase 0
  frameCount: number   // total frames in the sheet, laid out left-to-right top-to-bottom
  frameRate: number    // fps, e.g. 4 for idle, 10 for walk
  loop: boolean        // → repeat: -1 in Phaser when true
}
```

Variable-per-frame timing isn't needed for Phase 0 stubs and probably isn't needed for PixelLab output either; if it ever is, extend `frameCount: number` to `frames: { duration: number }[]`. Don't over-build it now.

Two separate artifact types, two separate generators (per D08):
- **Portrait** (`portraitUrl`) — one illustrated image per character, surfaces in the inspector, share cards, public viewer header. Generated by nano banana. `portraitStatus` mirrors the per-pose `status` so async generation in Phase 1 has a place to land.
- **Sprite poses** (`poses[].spriteSheetUrl`) — pixel-art sprite sheets, played on the Phaser stage. Generated by PixelLab.

The `portraitGenerator` and `generator` fields exist so Phase 0 stub assets (`'stub'`) and Phase 1+ real assets coexist without schema churn. The dispatch lives in `packages/shared/generators` per D09.

Indexes: `slug` unique, `(characterId, name)` unique on poses (one canonical idle/walk per character). Cascade-delete poses when a character is deleted so we never leak orphaned R2 objects via stale rows.

**Pose vocabulary (Phase 0):** fixed list — `idle`, `walk`, `attack`, `cast`. The "+ Add pose" picker shows only these. The API rejects unknown names with 400. Freeform pose names land in Phase 2+ once we know what PixelLab's vocabulary actually looks like; doing it now would create orphan stub-asset rows on every typo.

## API surface (all stubbed in Phase 0)

```
POST   /api/characters                    { prompt }                    → { slug, editKey }
GET    /api/characters/:slug              → public character + portrait + poses
PATCH  /api/characters/:slug              (auth) → updates attributes (no auto-regen)
POST   /api/characters/:slug/portrait     (auth) → 202, queues portrait gen (nano banana / stub)
POST   /api/characters/:slug/poses        (auth) { name }               → 202, queues sprite gen (pixellab / stub)
POST   /api/characters/:slug/voice        (auth) { text }               → audio URL  *(editor only in Phase 0)*
POST   /api/characters/:slug/rotate-key   (auth) → { editKey } (new)
GET    /api/stubs/v1/*                    public, serves files from apps/api/fixtures/stubs/v1/
```

**Auth model.** Every `(auth)` endpoint accepts the edit key from *either*:
- Header `X-Edit-Key: <editKey>` (used for direct API calls, scripts, tools), OR
- Cookie `sojourn_edit_<slug>=<editKey>` (HttpOnly, SameSite=Lax, set on first edit-URL visit)

The middleware tries the header first, falls back to the slug-namespaced cookie, hashes whichever it finds and compares to `editKeyHash`. Cookie stays HttpOnly so XSS can't steal it; the header path is for cases where JS needs to act on behalf of the user without reading the secret. **Cookie is namespaced per slug** (`sojourn_edit_<slug>`) so editing a second character doesn't lock you out of the first.

Other notes:
- **`refImage` removed from `POST /characters` in Phase 0** — reference upload UX is deferred (see "What's not in Phase 0"). Field returns in Phase 1 paired with the drop-zone UI.
- **`POST /voice` is editor-only in Phase 0.** The public viewer doesn't call it because anonymous read-access to voice isn't designed yet; reappears in Phase 2 alongside ElevenLabs.
- **`GET /api/stubs/v1/*`** is the only public read endpoint other than `GET /:slug`. Hosting stubs through the API (rather than `apps/web/public/`) means dev and prod use the same URL shape and the existing Vite proxy reaches them — no separate static-server setup.

Portrait and sprite generation are separate endpoints because they hit different providers, have different cost profiles, and fail independently. `PATCH` only updates DB rows — re-generation is always an explicit call, so the UI controls when to spend on a re-render.

`rotate-key` is for the "I leaked my edit URL" case: hits the same auth as any other write, mints a new `editKey`, replaces `editKeyHash`, invalidates the old. Endpoint exists in Phase 0 (it's part of validating the edit-key model); the UI affordance can land in Phase 1.

In Phase 0, both `/portrait` and `/poses` dispatch to the `'stub'` generator — `/portrait` returns the catalog `portrait.png` URL, `/poses` returns the matching sprite sheet keyed off `name` (rejecting any name not in the fixed vocabulary). `POST /voice` returns a 2-second pre-recorded mp3 URL. None of this hits a third-party.

## Stub asset catalog

Stubs are a load-bearing dependency for Phase 0 — spec them up front so the rest of the work has something deterministic to point at. They also become the offline-dev fallback when PixelLab / nano banana / ElevenLabs are unreachable in Phase 1+.

The catalog reflects the two-generator split: portraits are illustrated (representing nano banana output), sprites are pixel art (representing PixelLab output). Same character design across both so the visual continuity test is exercised by stubs.

Each pose ships as `<name>.png` plus `<name>.json` (the `PoseManifest` defined above):

| Asset | Filename | Dimensions | Manifest | Notes |
|---|---|---|---|---|
| Portrait | `stubs/v1/portrait.png` | 512×512, transparent PNG | — | Illustrated. Plain `<img>`, never through Phaser. |
| Idle | `stubs/v1/idle.png` + `.json` | 256×64 (4 frames × 64×64) | `frameCount: 4, frameRate: 4, loop: true` | Pixel art |
| Walk | `stubs/v1/walk.png` + `.json` | 512×64 (8 frames × 64×64) | `frameCount: 8, frameRate: 10, loop: true` | Pixel art |
| Attack | `stubs/v1/attack.png` + `.json` | 384×64 (6 frames × 64×64) | `frameCount: 6, frameRate: 12, loop: false` | Pixel art |
| Cast | `stubs/v1/cast.png` + `.json` | 384×64 (6 frames × 64×64) | `frameCount: 6, frameRate: 10, loop: false` | Pixel art |
| Voice clip | `stubs/v1/voice.mp3` | ~2 s, 22.05 kHz mono | — | Generic line, used for any text |

Storage and serving:
- **Local fixtures** at `apps/api/fixtures/stubs/v1/` — committed to repo, served by the API at `GET /api/stubs/v1/*` (see API surface). The Vite proxy already forwards `/api/*`, so the browser reaches these via the same URL in dev and prod.
- **R2** at `s3://sojourn-assets/stubs/v1/` — same files, served direct from R2's public URL.
- Switch via `STUB_SOURCE=local|r2` env var. The generator's `stub` impl returns either `/api/stubs/v1/<file>` or `<R2_PUBLIC_BASE_URL>/stubs/v1/<file>` accordingly. Default `local` in dev so the app boots with no R2 credentials.

Stubs round-trip through the real load path — same manifest shape, same URL pattern, same code in `<SpriteStage>`. No special-casing in the renderer.

## UI shell

Single editor page, four regions:

1. **Stage (center)** — Phaser canvas. Shows the currently selected pose (idle by default), played as a loop or once depending on `manifest.loop`. Pose dropdown above it.
2. **Portrait panel (top of inspector)** — plain `<img>` rendering `portraitUrl` (illustrated, not pixel art — never goes through Phaser). Explicit **"Regenerate portrait"** button calls `POST /characters/:slug/portrait`.
3. **Inspector (right)** — fields for `name`, `archetype` (select), `outfit` (text), `palette` (color chips), `expression` (chips), `voice` (placeholder dropdown). Each change calls `PATCH` immediately (saves only). Visual fields (`archetype`, `outfit`, `palette`, `expression`) additionally debounce-trigger `POST /portrait` after ~1.5 s of inactivity. **Sprite poses never auto-regen** — too expensive per call, user controls them via the pose grid. The inspector also contains a small "describe a change…" **AI-assist field** at top-right — stub in Phase 0 (appends to a local transcript only); real LLM lands in Phase 2+.
4. **Pose grid (bottom)** — cards for each pose with status (pending / ready / failed) and a thumbnail. "+ Add pose" picker is constrained to the fixed vocabulary (`idle`, `walk`, `attack`, `cast`) and calls `POST /poses`. Per-card "Regenerate" affordance also calls `POST /poses` for that name.

Public viewer page (`/c/:slug`) shows the portrait (plain `<img>`) and the Phaser stage with a pose dropdown — no inspector, no regenerate buttons, no edit affordances. **No voice button in Phase 0** (auth + persistence model for public-read voice isn't designed yet — reappears in Phase 2 with ElevenLabs).

## Concrete deliverables to call Phase 0 done

- [ ] `pnpm dev` boots Vite frontend + Node API + local SQLite, hot reloads both
- [ ] Hitting `/` and entering text creates a character, redirects to `/c/:slug/edit?key=…`
- [ ] Editor renders the stub portrait above the Phaser stage; the stage plays the idle pose on a loop
- [ ] Editing an inspector field calls `PATCH` and persists. Editing a *visual* field (archetype / outfit / palette / expression) additionally debounce-triggers `POST /portrait` and the portrait panel re-renders with the new (still stub) image. Non-visual fields (e.g. `name`) save without triggering regen.
- [ ] Clicking "Regenerate portrait" calls `POST /portrait` and the panel updates
- [ ] Adding a pose ("walk") via the pose grid calls `POST /poses`, the card shows in the grid, and the new pose plays on the stage
- [ ] Copying `/c/:slug` to incognito → public viewer renders portrait + Phaser stage with pose dropdown, no edit UI present, no voice button (Phase 0 scope)
- [ ] R2 is wired (even if stub assets are uploaded once and reused) — proves the upload path works before nano banana lands
- [ ] Drizzle migrations, env var loading, and a basic Hono auth middleware that checks `editKey` against `editKeyHash`
- [ ] One Playwright happy-path test that scripts the deliverables above (create → edit → add pose → public view in a fresh context). This is the regression net for Phase 1.
- [ ] `pino` set up server-side, with structured logs on every auth check (success + failure, including failed `editKey` attempts with hashed prefix only). No external observability stack — stdout is fine.
- [ ] `Generator` interface in `packages/shared/generators` with two implementations: `stub` (returns catalog assets) for both portraits and sprites. Real `pixellab` and `nano-banana` implementations land in Phase 1 — Phase 0 only needs the seam. API handlers dispatch via this interface; no provider-specific code in handlers.

## Decisions, push back if any are wrong

1. **No accounts in Phase 0** — edit-key-in-URL model. Simpler, ships faster. Risk: someone shares the wrong URL once and loses control. Mitigation: show a clear "this is the edit URL — keep it private" banner on first creation.
2. **R2 over Vercel Blob** — R2 is cheaper at scale and you're not locked to Vercel. Costs you ~30 min of S3 SDK setup vs. Blob's drop-in.
3. **Hono over Express** — smaller, TS-first, runs the same on Node and Bun, easier WebSocket later. If you have a strong Express preference, switch.
4. **Turso over Postgres** — for a single-user-creation prototype, SQLite is plenty and Turso gives you replication if you need it. Switch to Neon later if relations grow.
5. **Phaser as the renderer, but rendered as a *component*** — wrap Phaser in a single `<SpriteStage>` React component, expose `setPose(name)` / `play()` as imperative handles. Keeps Phaser sandboxed; rest of app is plain React.

## Infra glue

Cross-origin and env scaffolding that's painful to retrofit:

**Local dev (same-origin via Vite proxy):**
- Vite dev server proxies `/api/*` → `http://localhost:3000` so the browser sees a single origin
- Cookies use `SameSite=Lax`, `HttpOnly`, no `Secure` flag in dev (so localhost works). HttpOnly is fine because auth middleware also accepts the `X-Edit-Key` header — JS never needs to read the cookie. See "Auth model" in the API surface section.
- Cookie name is namespaced per slug: `sojourn_edit_<slug>=<editKey>`, so editing a second character doesn't overwrite the first's auth.
- No CORS config needed locally because of the proxy.

**Production (cross-origin):**
- Frontend on Vercel domain, API on Fly/Railway domain → real CORS
- `Access-Control-Allow-Origin` set to the frontend domain, `Allow-Credentials: true`
- Cookies switch to `SameSite=None; Secure`, still HttpOnly, still slug-namespaced
- One `CORS_ORIGIN` env var on the API picks the allowed origin per environment

**Env var scaffolding** (`.env.example` checked in, `.env.local` gitignored):

```
# DB
DATABASE_URL=                # libsql://… (Turso) or file:./local.db (dev)

# Storage
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=sojourn-assets
R2_PUBLIC_BASE_URL=          # https://assets.sojourn.app or pub-…r2.dev

# Stub source (Phase 0)
STUB_SOURCE=local            # local | r2

# Auth & app
EDIT_KEY_PEPPER=             # used when hashing edit keys
CORS_ORIGIN=http://localhost:5173
MIGRATE_ON_BOOT=1            # run drizzle migrations on Node startup; off in CI

# Phase 1+ placeholders (unused in Phase 0 but documented now)
PIXELLAB_API_KEY=
GEMINI_API_KEY=
ELEVENLABS_API_KEY=
```

**Migrations:** Drizzle migrations run on Node startup when `MIGRATE_ON_BOOT=1`. CI runs them as a separate step against a temp DB. Production: same boot-time migration is fine until we have multiple instances; revisit if/when we scale horizontally.

**Deploy sequencing:** picking Fly vs Railway is deferred until the first deploy actually happens — committing to one now is theater. Whichever wins, the contract is: `pnpm --filter api build && pnpm --filter api start`, with `MIGRATE_ON_BOOT=1` so the schema is current before the server accepts traffic.

## What's *not* in Phase 0 (and why)

- Real PixelLab calls (sprites) → Phase 1
- Real nano banana calls (portraits) → Phase 1
- Real ElevenLabs → Phase 2
- Lipsync of any kind → Phase 2
- AI-assist field actually working → Phase 2+
- Reference-image upload UX (drop zone, preview) → Phase 1, paired with nano banana
- Public gallery / discovery → Phase 3

## Estimated effort

~3 days of focused work (2–3 days for the original scaffold, +half day for the Playwright test / stub catalog / logging / infra glue, +couple hours for the generator abstraction). The scaffolding is mostly mechanical; the things worth thinking carefully about are the edit-key flow (changing it later means migrating URLs) and the generator interface shape (it's the seam Phase 1 lands on).
